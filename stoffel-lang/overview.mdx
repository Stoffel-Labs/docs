---
title: "StoffelLang Overview"
description: "The current Stoffel language for clear local logic and MPC-aware computations compiled to Stoffel VM bytecode."
---

StoffelLang is the application language compiled by the `stoffellang` crate in the `stoffel` repository. It uses Python-like indentation, `def` functions, `var` bindings, static type annotations, lists, objects, closures, and explicit MPC share APIs.

The Stoffel 0.1.0 language is practical and still evolving. Prefer examples from `crates/stoffel-lang/examples/` and the Rust SDK examples over syntax outside the documented 0.1.0 `def`/`var`/`.stflb` workflow.

## Design goals

- Familiar Python-style control flow and indentation.
- Static types with inference for local development ergonomics.
- Direct compilation to portable `.stflb` Stoffel VM bytecode.
- Explicit share-oriented MPC APIs such as `Share.*`, `ClientStore.*`, `Mpc.*`, and `MpcOutput.*`.
- Host integration through the CLI and Rust SDK.

## A minimal program

```stoffel
def add(a: int64, b: int64) -> int64:
  return a + b

def main() -> int64:
  return add(40, 2)
```

Compile and run it from a project or as a source file:

```bash
stoffel check src/main.stfl
stoffel build
stoffel run --entry main
```

## Variables and types

Use `var` for local bindings. Type annotations are optional when the compiler can infer the type.

```stoffel
def main() -> int64:
  var count: int64 = 42
  var message = "hello"
  var ready = true
  discard message
  discard ready
  return count
```

Current primitive and runtime-facing types include:

- signed integers: `int8`, `int16`, `int32`, `int64`
- unsigned integers: `uint8`, `uint16`, `uint32`, `uint64`
- `bool`
- `string`
- `None` / no-value returns
- `Share` for MPC secret shares
- `list[T]`
- object and closure values used by the VM runtime

## Functions

```stoffel
def multiply(a: int64, b: int64) -> int64:
  return a * b

def log_message(message: string) -> None:
  print(message)
```

The CLI and SDK default to executing `main`, but most commands can select another entry function:

```bash
stoffel run --entry multiply --input a=6 --input b=7
```

## Control flow

```stoffel
def fibonacci(n: int64) -> int64:
  if n <= 1:
    return n

  var previous: int64 = 0
  var current: int64 = 1
  var index: int64 = 2

  while index <= n:
    var next: int64 = previous + current
    previous = current
    current = next
    index = index + 1

  return current
```

Ranges and list iteration are supported:

```stoffel
def checksum(limit: int64) -> int64:
  var total: int64 = 0

  for value in 1..limit:
    total += value

  return total
```

## Lists

```stoffel
def main() -> int64:
  var scores: list[int64] = []
  scores.append(72)
  scores.append(41)
  scores.append(93)

  scores[1] = scores[1] + 10

  var total: int64 = 0
  for score in scores:
    total += score

  return total + scores.len()
```

## MPC share APIs

The 0.1.0 release models MPC values explicitly as `Share` values. Use `ClientStore` to read client-provided shares, `Share` methods/functions to compute, and `open()` or `MpcOutput.send_to_client` where your program intentionally reconstructs or delivers a result.

```stoffel
def add_private_values(a: Share, b: Share) -> int64:
  var sum = Share.add(a, b)
  return sum.open()
```

Equivalent method-style calls are common in examples:

```stoffel
def normalize(raw_score: Share) -> Share:
  var adjusted = raw_score.add_scalar(25)
  return adjusted.mul_scalar(2)
```

## ClientStore inputs

`ClientStore` exposes local or network client input slots to the program:

```stoffel
def main() -> int64:
  var client_count = ClientStore.get_number_clients()
  var share = ClientStore.take_share(0, 0)
  var opened: int64 = share.open()
  return opened + client_count
```

When running locally through the CLI, provide client-slot inputs with `--client-input`:

```bash
stoffel run --local --client-input 0=42 --parties 5 --threshold 1
```

With the Rust SDK, use `.with_client_input(0, &[42_i64])`.

## Runtime metadata

MPC programs can query runtime metadata and capabilities:

```stoffel
def main() -> int64:
  var score: int64 = Mpc.party_id()
  score = score + Mpc.n_parties()
  score = score + Mpc.threshold()

  if Mpc.has_capability("multiplication"):
    score = score + 100

  discard Mpc.protocol_name()
  discard Mpc.curve()
  discard Mpc.field()
  discard Mpc.instance_id()
  return score
```

## Compilation outputs

The current bytecode extension is `.stflb`:

```bash
stoffel compile src/main.stfl --output target/debug/main.stflb
stoffel compile --disassemble target/debug/main.stflb
```

`stoffel build` writes project bytecode under `target/debug` or `target/release` depending on the selected profile.

## Examples in the repository

Useful source-of-truth examples live under `crates/stoffel-lang/examples/`:

- `local_control_flow/main.stfl`
- `local_collections/main.stfl`
- `mpc_share_arithmetic/main.stfl`
- `mpc_runtime_info/main.stfl`
- `mpc_client_private_score/main.stfl`
- `mpc_aes128_cbc_client_io/main.stfl`
- `threshold_signatures/*/main.stfl`

## Next steps

- [Syntax and Examples](./syntax)
- [Compilation](./compilation)
- [Built-in Functions](../stoffel-vm/builtins)
- [Rust SDK Examples](../rust-sdk/examples)
